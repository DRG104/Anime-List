{% layout 'layout.liquid' %}


{% block content %}
<!-- <div class="container">
    <form action="/anime/search" method="POST">
        <fieldset class="start">
            <legend>Search Anime</legend>
            <label>Anime</label>
            <input 
                type="text" 
                name="anime"
            >
            <input 
            type="submit" 
            value="Search" 
            >
        </fieldset>
    </form>
</div> -->
    <h1>Here are some popular anime!</h1>
    {% for index in (0..24) %}
        <div class="card" style="width: 18rem">
            <h3>Mal ID is: 1 {{anime.data[index].mal_id}}</h3>
            <!-- <h3>Info is:{{data.data[0].images.jpg.image_url}}</h3> -->
            <h3>Anime Title is:{{anime.data[index].title}}</h3>
            <a href="/anime/{{anime.data[index].mal_id}}">
                <img src="{{anime.data[index].images.jpg.image_url}}">
            </a>
        </div>
    {% endfor %}

{% endblock %}